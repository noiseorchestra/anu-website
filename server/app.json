{
    "scripts": {
      "dokku": {
        "predeploy": "python /code/server/manage.py collectstatic --noinput",
        "postdeploy": "ssh-keygen -f /root/.ssh/id_rsa -t rsa -N ''"
      }
    }
  }