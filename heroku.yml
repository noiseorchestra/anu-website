setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web:
      dockerfile: Dockerfile
      target: server-prod
release:
  image: web
  command:
    - ./on-release.sh
run:
  web: gunicorn config.wsgi
